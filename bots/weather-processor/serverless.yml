weather-processor:
  handler: bots/weather-processor/index.handler
  description: Bot generated by serverless-leo
  memorySize: 256
  timeout: 300
  role: BotRole
  events:
    - leo:
        queue: ${self:service}.weather
        destination: ${self:service}.weather-transformed
